import 'package:flutter/material.dart';

class Palette {
  // Màu chủ đạo (Xanh cổ vịt - Tạo cảm giác thiên nhiên, sang trọng)
  static const Color primary = Color(0xFF006491);

  // Màu điểm nhấn (Cam san hô - Kích thích nút bấm "Đặt ngay")
  static const Color accent = Color(0xFFFF6B6B);

  // Màu nền (Trắng hơi xám nhẹ cho dịu mắt)
  static const Color background = Color(0xFFF9F9F9);

  // Màu chữ
  static const Color textMain = Color(0xFF2D3142);
  static const Color textSub = Color(0xFF909399);
}